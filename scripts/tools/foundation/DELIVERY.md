# Delivery Notes

- Path: `scripts/tools/foundation/`
- Public source entrypoint: `src/index.ts`
- Include policy: `src/**/*.ts`, `test/**/*.ts`, `README.md`, `CHANGELOG.md`, `DELIVERY.md`, `package.json`, `tsconfig.json`
- Owners: README/changelog/revision policy stay with this subrepo.
- Rollback: revert Docnav caller imports to the previous `scripts/tools/*` implementations or reset the subrepo pin to the last verified revision.
