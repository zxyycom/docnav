id: rust-native-test
message: Supported Rust test entity
severity: info
language: Rust
rule:
  all:
    - pattern:
        context: |
          fn $NAME() { $$$BODY }
        selector: function_item
    - follows:
        pattern: '#[test]'
        stopBy: neighbor
