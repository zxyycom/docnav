### Case AUX-SMOKE-HARNESS-011: Uses semantic config fixtures with the shared Markdown document

Entry:
- `test/smoke/core/fixtures/project.test.ts > core smoke fixture projects > uses semantic config fixtures with the shared Markdown document`

Contract:
- `docs/testing.md` 定义或约束“Core smoke config fixture helper 保持配置/文档分层”所涉及的稳定行为边界。

Proves:
- 原生入口 `core smoke fixture projects > uses semantic config fixtures with the shared Markdown document` 直接验证“Uses semantic config fixtures with the shared Markdown document”所描述的结果。
