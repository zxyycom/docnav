### Case AUX-PARALLEL-RUNNER-002: Normalizes task metadata and supports task.run as the execution body

Entry:
- `scripts/tools/parallel-task-runner/test/index.test.ts > parallel task runner > normalizes task metadata and supports task.run as the execution body`

Contract:
- `docs/testing.md` 定义或约束“Parallel task runner 保持调度契约”所涉及的稳定行为边界。

Proves:
- 原生入口 `parallel task runner > normalizes task metadata and supports task.run as the execution body` 直接验证“Normalizes task metadata and supports task.run as the execution body”所描述的结果。
