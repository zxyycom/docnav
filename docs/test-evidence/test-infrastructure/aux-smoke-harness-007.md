### Case AUX-SMOKE-HARNESS-007: Records default runner stdout and stderr on command records

Entry:
- `test/tools/smoke-harness.test.ts > smoke harness task scheduling > records default runner stdout and stderr on command records`

Contract:
- `docs/testing.md` 定义或约束“Smoke harness 正确记录 task 和 command 输出语义”所涉及的稳定行为边界。

Proves:
- 原生入口 `smoke harness task scheduling > records default runner stdout and stderr on command records` 直接验证“Records default runner stdout and stderr on command records”所描述的结果。
