### Case AUX-SMOKE-HARNESS-008: Runs default runner commands with plain text output environment

Entry:
- `test/tools/smoke-harness.test.ts > smoke harness task scheduling > runs default runner commands with plain text output environment`

Contract:
- `docs/testing.md` 定义或约束“Smoke harness 正确记录 task 和 command 输出语义”所涉及的稳定行为边界。

Proves:
- 原生入口 `smoke harness task scheduling > runs default runner commands with plain text output environment` 直接验证“Runs default runner commands with plain text output environment”所描述的结果。
