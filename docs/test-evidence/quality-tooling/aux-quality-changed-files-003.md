### Case AUX-QUALITY-CHANGED-FILES-003: Keeps current, changed, and baseline repository files aligned

Entry:
- `scripts/tools/quality-core/src/input/files.test.ts > quality changed file input > keeps current, changed, and baseline repository files aligned`

Contract:
- `docs/tooling.md` 定义或约束“Quality revision inputs 保持 current/changed/baseline 一致”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality changed file input > keeps current, changed, and baseline repository files aligned` 直接验证“Keeps current, changed, and baseline repository files aligned”所描述的结果。
