### Case AUX-QUALITY-CODE-AREAS-004: Classifies root workspace crates by Rust source role

Entry:
- `scripts/quality/config.test.ts > quality code area classification > classifies root workspace crates by Rust source role`

Contract:
- `docs/tooling.md` 定义或约束“Quality code area 分类稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality code area classification > classifies root workspace crates by Rust source role` 直接验证“Classifies root workspace crates by Rust source role”所描述的结果。
