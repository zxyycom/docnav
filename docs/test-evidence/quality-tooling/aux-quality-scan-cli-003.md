### Case AUX-QUALITY-SCAN-CLI-003: Keeps quick quality checks baseline free and explicit

Entry:
- `scripts/quality/args.test.ts > quality scan CLI args > keeps quick quality checks baseline-free and explicit`

Contract:
- `docs/tooling.md` 定义或约束“Quality scan CLI 默认值稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality scan CLI args > keeps quick quality checks baseline-free and explicit` 直接验证“Keeps quick quality checks baseline free and explicit”所描述的结果。
