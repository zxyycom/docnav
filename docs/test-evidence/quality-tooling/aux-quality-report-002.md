### Case AUX-QUALITY-REPORT-002: Keeps changed file watchlist useful without baseline annotations

Entry:
- `scripts/tools/quality-core/src/output/report/markdown-report.test.ts > quality report > keeps changed-file watchlist useful without baseline annotations`

Contract:
- `docs/tooling.md` 定义或约束“Quality report 排名和 changed-file 摘要稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality report > keeps changed-file watchlist useful without baseline annotations` 直接验证“Keeps changed file watchlist useful without baseline annotations”所描述的结果。
