### Case AUX-QUALITY-CODE-AREAS-003: Keeps smoke harness infrastructure in the validation/smoke area

Entry:
- `scripts/quality/config.test.ts > quality code area classification > keeps smoke harness infrastructure in the validation/smoke area`

Contract:
- `docs/tooling.md` 定义或约束“Quality code area 分类稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality code area classification > keeps smoke harness infrastructure in the validation/smoke area` 直接验证“Keeps smoke harness infrastructure in the validation/smoke area”所描述的结果。
