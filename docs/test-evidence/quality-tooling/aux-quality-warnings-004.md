### Case AUX-QUALITY-WARNINGS-004: Adds configured accepted reasons without relying on duplicate line numbers

Entry:
- `scripts/tools/quality-core/src/output/warnings/generator.test.ts > quality warning generation > adds configured accepted reasons without relying on duplicate line numbers`

Contract:
- `docs/tooling.md` 定义或约束“Quality warning 阈值语义稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality warning generation > adds configured accepted reasons without relying on duplicate line numbers` 直接验证“Adds configured accepted reasons without relying on duplicate line numbers”所描述的结果。
