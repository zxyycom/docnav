### Case AUX-QUALITY-CACHE-002: Keys duplicate code cache by scan identity and strips changed scope annotations

Entry:
- `scripts/tools/quality-core/src/measurement/cache.test.ts > quality measurement cache > keys duplicate-code cache by scan identity and strips changed-scope annotations`

Contract:
- `docs/tooling.md` 定义或约束“Quality measurement cache identity 稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality measurement cache > keys duplicate-code cache by scan identity and strips changed-scope annotations` 直接验证“Keys duplicate code cache by scan identity and strips changed scope annotations”所描述的结果。
