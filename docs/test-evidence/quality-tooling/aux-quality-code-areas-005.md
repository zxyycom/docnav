### Case AUX-QUALITY-CODE-AREAS-005: Discovers representative Rust and TypeScript sources in the root workspace

Entry:
- `scripts/quality/config.test.ts > quality code area classification > discovers representative Rust and TypeScript sources in the root workspace`

Contract:
- `docs/tooling.md` 定义或约束“Quality code area 分类稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `quality code area classification > discovers representative Rust and TypeScript sources in the root workspace` 直接验证“Discovers representative Rust and TypeScript sources in the root workspace”所描述的结果。
