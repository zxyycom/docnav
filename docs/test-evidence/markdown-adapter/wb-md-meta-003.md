### Case WB-MD-META-003: Definition declares manifest and full read capabilities

Entry:
- `crates/adapters/markdown/tests/adapter/meta.rs > definition_declares_manifest_and_full_read_capabilities`

Contract:
- `docs/adapters/markdown.md` 定义或约束“Markdown manifest/probe/info 元数据稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `definition_declares_manifest_and_full_read_capabilities` 直接验证“Definition declares manifest and full read capabilities”所描述的结果。
