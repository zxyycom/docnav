### Case WB-MD-META-002: Manifest declares markdown v0 identity and formats

Entry:
- `crates/adapters/markdown/tests/adapter/meta.rs > manifest_declares_markdown_v0_identity_and_formats`

Contract:
- `docs/adapters/markdown.md` 定义或约束“Markdown manifest/probe/info 元数据稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `manifest_declares_markdown_v0_identity_and_formats` 直接验证“Manifest declares markdown v0 identity and formats”所描述的结果。
