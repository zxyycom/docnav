### Case WB-PROTO-DIAGNOSTICS-003: Navigation routing default guidance uses static registry language

Entry:
- `crates/shared/protocol/src/tests/basic.rs > navigation_routing_default_guidance_uses_static_registry_language`

Contract:
- `docs/protocol.md` 定义或约束“Protocol diagnostic mapping and projection 保持稳定”所涉及的稳定行为边界。

Proves:
- 原生入口 `navigation_routing_default_guidance_uses_static_registry_language` 直接验证“Navigation routing default guidance uses static registry language”所描述的结果。
