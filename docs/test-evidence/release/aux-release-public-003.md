### Case AUX-RELEASE-PUBLIC-003: Stages the exact Windows public file set from canonical package evidence

Entry:
- `scripts/tools/release-package/public.test.ts > stages the exact Windows public file set from canonical package evidence`

Contract:
- `docs/testing/release.md` 定义或约束“Public files 从已验证 canonical package 派生”所涉及的稳定行为边界。

Proves:
- 原生入口 `stages the exact Windows public file set from canonical package evidence` 直接验证“Stages the exact Windows public file set from canonical package evidence”所描述的结果。
