### Case AUX-RELEASE-ARGS-004: Package selection keeps explicit manifest support

Entry:
- `scripts/tools/release-package/args.test.ts > package selection keeps explicit manifest support`

Contract:
- `docs/testing/release.md` 定义或约束“Release package 参数解析保持边界”所涉及的稳定行为边界。

Proves:
- 原生入口 `package selection keeps explicit manifest support` 直接验证“Package selection keeps explicit manifest support”所描述的结果。
