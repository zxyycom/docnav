# OpenSpec 历史材料

本目录保存 Docnav 在 2026-08-11 切换到 Change Plan 前形成的 OpenSpec 材料，只用于审计形成时设计、任务和验收上下文。

- 当前稳定规范由 [`docs/`](../../../docs/navigation.md) 指向的 owner 文档拥有。
- 跨 change 的已确认方向由 [`docs/decisions/`](../../../docs/decisions/) 拥有。
- 当前 change 计划位于 [`changes/`](../../../changes/)；切换时仍有交付价值的内容按实际成熟度迁入完整 Change Plan，当前 stage、assessment 和恢复门禁只读取新目录及 `change-plan` skill，不从本归档推断。
- 调查快照位于 [`docs/investigations/`](../../../docs/investigations/)。

本目录的 `changes/` 下未完成 OpenSpec 目录、`.openspec.yaml`、capability spec 和 OpenSpec CLI 显示的状态都只是切换时历史，不表示当前 active plan、实施授权、未来方向或 Current 能力。不要继续同步、验证或归档这些材料；保留内部布局和可复核链接只为历史回顾。
