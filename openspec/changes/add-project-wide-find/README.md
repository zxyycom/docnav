# add-project-wide-find

**这是一个临时 Target planning artifact：目标是让无 path 的 `docnav find` 确定性搜索 resolved project root，同时保持显式 path 的 single-document find；当前 completed tasks 为 0/43，blocking gates 尚未关闭，因此它既不是 Current 行为，也不是实施授权。**
