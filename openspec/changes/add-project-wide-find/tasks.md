**本任务清单只在两个 predecessor handoff、traversal dependency decision 和 feasibility audit 全部显式通过后，实施省略 path 的有限 project-wide find；当前所有任务均未完成，artifact-ready 不等于获准实现。**

**阻断规则：1.1–1.7 全部勾选前，不得执行第 2 节及之后的 docs、schema、production代码、产品测试、Cargo、lockfile 或 release 修改。任何 predecessor final contract、人工 dependency decision 或 private quantum validation 与本 change 不一致时，先修订本 change 并重新 strict validate。**

## 1. Blocking artifact, predecessor, dependency, and feasibility gates

- [ ] 1.1 在 change 目录创建 purpose-named `audit-report.md`，逐项核对 proposal、design、五个 delta specs 和 tasks 都围绕 optional path → current project root、legacy document request + explicit project request union、path + opaque ref、final inference/manifest seam、per-directory DFS、fixed-quantum numeric replay、partial/fatal split 和 no-auto-read；确认 capability IDs 仍是新增 `project-find` 与修改 `core-cli`、`navigation-input-resolution`、`protocol-contract`、`output-contract`，Open Questions 有 owner/关闭动作，且未把 proposed/predecessor 状态写成 Current implementation。
- [ ] 1.2 取得 `redesign-find-result-model` owner 对本 change exact wrapper handoff 的显式 acceptance，并核对该 change 已完成 final delta rewrite、blocking audit、implementation 和 validation；从最终 owner docs/schema/Rust types恢复 single-document logical unit、order/page 和 auto-read seam，只更新本 change 的 generic handoff，不复制 provisional occurrence/distinct/group 字段。任一条件未满足时记录阻塞并停止。
- [ ] 1.3 取得 `replace-probe-traversal-with-inferred-routing` owner 对本 change exact handoff 的显式 acceptance，并核对其已完成 final normalized format → exact registry mapping、explicit adapter exact lookup、manifest descriptor semantics、failure taxonomy、implementation 和 validation；确认 project automatic 每个候选只调用一次 final inference seam，explicit branch不调用 inference，且本 change不恢复 candidate-execution fallback。任一条件未满足时记录阻塞并停止。
- [ ] 1.4 在 change 目录创建 `dependency-audit.md`，用同一证据表比较至少一个成熟 gitignore-aware walker、可行 alternatives 和 no-new-dependency baseline；记录 ecosystem adoption、maintenance/release cadence、security advisories/unsafe/transitives、license/notice、workspace toolchain/MSRV、Linux/Windows targets、minimal features、dependency/binary/package size、cold/warm startup、ignore/symlink/per-directory DFS correctness、rollback成本和推荐结论。
- [ ] 1.5 由用户或指定 architecture/product owner 显式批准 exact traversal crate、version requirement、features 和接受的维护/security/license/MSRV/targets/package/startup trade-offs，或批准 no-new-dependency implementation；把批准人、日期和 exact outcome写入 `dependency-audit.md`。此 task 勾选前不得修改任何 Cargo manifest、lockfile或 production traversal code。
- [ ] 1.6 对 current source 和获批 traversal path 做实现可行性 proof：closed request union保持旧 document encoding、project root无 synthetic path；per-directory sorted DFS只做 directory-local buffering；automatic/explicit routing和 local/fatal taxonomy可复用 final predecessor seam；正数、有限、同一build固定且exact value implementation-private的quantum驱动 `(document_position, adapter_page, logical_unit_offset)` 每个 transition推进；empty-but-continuable adapter page可形成empty continuation；adapter `limit`不变；无全项目预收集、opaque cursor、cross-run cache或project-aware adapter operation。任何 proof 缺失时先修订artifacts或取得owner decision。
- [ ] 1.7 在 change-local `work-quantum-validation.md` 中用 representative empty/filter-heavy/multi-page/local-failure workloads 比较并选择 current implementation 的初始 private quantum；证明值严格大于0、每页有限、所有非fatal step推进、同一build与稳定输入的replay边界一致、empty continuation可达且无busy loop。记录命令/fixture/build和选择理由，但不得把exact value写入public docs/spec/schema/examples、CLI/config/protocol或compatibility承诺；该validation通过前不得实现production pagination。

## 2. Long-term owners, raw protocol schemas, and examples

- [ ] 2.1 按 `docs/navigation.md` owner mapping 更新 `docs/architecture.md`、`docs/cli.md`、`docs/navigation-input-resolution.md`、`docs/protocol.md` 和 `docs/output.md`，分别落地 scope/owner、optional path、routing/replay、request/result/page/failure 和 readable projection；其它文档只保留摘要与 owner 链接。
- [ ] 2.2 在 `docs/navigation.md` reading path、rule ownership 和 terminology 中加入 `project-find` 稳定入口，同时明确 OpenSpec/predecessor 状态不构成 Current binary evidence。
- [ ] 2.3 更新 `docs/schemas/protocol-request.schema.json` 和 runtime typed decode/validation：将 raw find request定义为 closed `SingleDocumentFindRequest | ProjectFindRequest`；前者保留 exact existing `document.path` encoding，后者 required `project.root` 且 forbidden `document`，两者继续复用 closed `FindArguments`，both/neither target严格失败。
- [ ] 2.4 将 predecessor final single-document item schema作为 nested `match` 的唯一引用，更新 `docs/schemas/protocol-response.schema.json` 和 runtime validation，新增 closed `scope: "project"` result、bounded failures和numeric page；schema不得编码private quantum exact value，current runtime只按其build-private正数有限quantum验证/形成有限wrappers，且不复制predecessor字段。
- [ ] 2.5 更新 `docs/schemas/json-schema.md` 与 `docs/examples/contract-examples.md`；保持现有 `protocol-find-request.json` unchanged/valid，新增 project request example with `project.root`，并新增 multi-document same-ref、mixed match/failure、empty-continuable adapter page和terminal empty response examples。
- [ ] 2.6 运行 request/response schema/example validation并执行 Manual CR：逐字段对照 owner docs、public schemas、Rust closed enums、concrete decode wrapper、predecessor nested unit、existing diagnostic projection和private finite-wrapper runtime guard；确认schema/examples未泄漏exact quantum，且无synthetic document path、placeholder type、presentation-only identity或path-in-ref。

## 3. Core CLI scope and approved deterministic traversal

- [ ] 3.1 将 `find` positional path改为 optional，更新 strict argv/help/preflight，使 supplied file、omitted path、explicit directory、extra positional、missing query和project-inapplicable `--auto-read`进入规范 branch，且其它 document commands不变。
- [ ] 3.2 扩展 core-to-navigation command model，使 project scope传递 existing `ProjectContext.project_root` 而不构造 document path；single-document normalization、outside-root support、config descriptors和existing raw document request保持原样。
- [ ] 3.3 只实施 task 1.5 明确批准的 traversal path；若批准 dependency，Cargo/lockfile只加入 exact crate/version/features并记录 transitive/license/MSRV/targets evidence；若批准 no-new-dependency，不得加入 substitute crate、custom ignore language或 wrapper framework。
- [ ] 3.4 实现 lazy per-directory sorted deterministic DFS：每个目录只 buffer/sort immediate entries，按 case-sensitive UTF-8 normalized segment bytes depth-first访问；只读 project `.gitignore`/nested `.gitignore`/`.ignore`、不读 global ignore、ordinary hidden included、control dirs excluded、symlink skipped、no size skip，且禁止 flat all-project collection/sort。
- [ ] 3.5 实现 traversal diagnostics：unrepresentable/colliding identity、root/nested directory/owned-ignore enumeration failure为 fatal；unique identity后的 file metadata/open failure为 local；不得用 lossy display path、silent incomplete result或 project-root escape。
- [ ] 3.6 为 CLI/root/traversal补齐 focused tests，覆盖 nearest `.docnav`、cwd fallback、explicit path unchanged/directory rejected、nested ignore、global ignore无效、hidden included、control dir excluded、file/directory symlink skipped、large file不按size skip、per-directory DFS order、directory-local buffering proof、fatal identity/enumeration和local file failure。

## 4. Navigation routing, wrapping, failures, and replay

- [ ] 4.1 在 full config/catalog/registry validation 后实现 automatic project routing：每个project request/replay对每个eligible file恰好一次final inference，normalize为predecessor-owned format id并exact-match registry；invocation-local selection跨该document adapter pages复用，later request重放时重新推断且无cross-run selection cache；exact `Unknown`与unregistered format普通过滤，candidate inference I/O、multiple identities的`FORMAT_AMBIGUOUS`和unclassified `format-routing-failed`各为bounded local failure并推进document；只有duplicate/conflicting static registry format-identity invariant为routing-global fatal，registry order不得选winner。
- [ ] 4.2 实现 explicit adapter routing：traversal前 exact id lookup，missing id只返回existing `ADAPTER_UNAVAILABLE` / `ADAPTER_NOT_FOUND`；duplicate adapter id由static registry validation在caller exact lookup前拒绝；跳过inference，用selected definition final manifest format descriptors和owner-approved comparison semantics预筛path eligibility；descriptor mismatch普通过滤，eligible candidate仍执行真实selected parse/find且失败为local，不fallback。
- [ ] 4.3 在 selected adapter确定后构造 per-document catalog view和existing closed `FindInput`；common fields用于每个document，exact adapter-tagged fields只进入匹配 view，adapter只观察 document/query/adapter page/limit/options，不观察project root/page/state/output/auto-read。
- [ ] 4.4 将 predecessor final logical units包为独立 `document.path` + nested `match`，保持 adapter order/ref byte equality；same ref across documents不去重、不拼接、不解析。
- [ ] 4.5 实现local/fatal mapping：每个unique document最多一个local existing diagnostic projection并推进document；inference I/O、`FORMAT_AMBIGUOUS`、unclassified `format-routing-failed`和selected parse/result failure均local；unknown/unsupported/descriptor mismatch/no-match无failure；root/traversal/ignore/global input/config/catalog/explicit lookup、duplicate/conflicting static registry format identity和project validation为top-level fatal。
- [ ] 4.6 用task 1.7选出的implementation-private正数有限quantum实现fixed-within-build state machine和`(document_position, adapter_page, logical_unit_offset)`；filter/terminal推进document，match推进offset，continuable page推进adapter page，local failure记录并推进document；每个transition最多一个wrapper且必须推进，wrappers受private quantum有限约束，adapter `limit`原样传入且不作为outer quota；public type/schema/example不得暴露exact value。
- [ ] 4.7 实现numeric page deterministic replay：page `n`从初态以同一build的private quantum重放preceding `n-1` logical steps后执行当前finite step；stable state无丢失/重复，quantum未证明terminal时返回page+1，beyond-end返回empty/null；empty-but-continuable adapter page必须推进并允许empty project response + continuation；不得precollect project、persist cursor/result set/snapshot或cross-run cache。
- [ ] 4.8 排除 project scope auto-read projection；explicit project `--auto-read`在traversal前严格失败，valid config只做full validation，ProjectFindResult后不做unique-ref计算或nested read。

## 5. Protocol, output, logging, and process mapping

- [ ] 5.1 在 shared protocol加入 closed raw find request enum（legacy document branch + project-root branch）、closed `ProjectFindResult`、document match wrapper和document failure wrapper，并扩展 operation/request/result validation；保持 existing document encoding、opaque ref、existing error object和single response envelope。
- [ ] 5.2 更新 `ProtocolJson` integration，使 project request/success/fatal failure仍各自处理一个 JSON value且stdout purity不变；local failures保持nested success facts而非stderr-only或sibling envelopes。
- [ ] 5.3 实现 built-in readable project mapping，展示scope/page、独立document path、完整opaque ref、predecessor final unit presentation以及每个local failure path/code/message；禁止synthesized path-ref identity、hidden failure和auto-read block。
- [ ] 5.4 更新 core process mapping，使 validated match-only、mixed、failure-only和empty-continuable project results退出`0`，fatal project/root/registry/output failures沿用existing non-zero mapping。
- [ ] 5.5 审计 invocation logging，只复用bounded project-root/document/operation/status metadata；不得记录完整query/ref/project result/failure array、inference raw facts、traversal progress或新增stdout/stderr event。

## 6. Test evidence and public behavior proof

- [ ] 6.1 修改任何测试前按 `docs/testing.md`、行为 owner、`docs/testing/case-maintenance.md` 和 `test-evidence-review` skill恢复责任，运行 `bun run test-evidence -- check --root .` 证明完整current tree static/runtime/Case闭合，并用topics/list/show定位复用/新增 Cases。
- [ ] 6.2 为core/navigation Rust tests补齐scope、request target、per-directory DFS、automatic once-inference、unknown/unsupported filter、inference I/O/`FORMAT_AMBIGUOUS`/unclassified routing local advance、static registry identity conflict fatal、explicit descriptor filter/real parse、same-ref different-document、mixed/failure-only/fatal、private finite-transition state、empty adapter continuation、terminal replay和no-auto-read。
- [ ] 6.3 为protocol/runtime tests补齐legacy document request backward compatibility、valid project request、both/neither target rejection、closed result decode/validation、nested error projection、private finite-wrapper guard、page+1/null、same-build replay、empty continuation和ref/path independent pass-through；确认schema/example不编码exact quantum。
- [ ] 6.4 为 built-in renderer conformance补齐project readable exact facts、mixed failures、empty continuation和no-auto-read block；保留同一 response 的 protocol-json isolation proof。
- [ ] 6.5 更新 Semantic Cases 的 Owner/Proves/Entities，保持existing single-document Cases语义连续；运行最窄 runner后再次运行 `bun run test-evidence -- check --root .` 证明完整闭合。
- [ ] 6.6 更新真实 CLI smoke和multi-format fixtures，覆盖 pathless current-root、explicit path unchanged、ignored/symlink candidates、automatic and explicit routing、same ref in two files、mixed local failure、empty adapter page、multi-page replay、request/response schemas、readable/stdout/stderr和exit behavior。

## 7. Integration verification and handoffs

- [ ] 7.1 运行 changed Rust crates format、targeted tests、clippy/check和request/response schema/example validation；对获批 traversal path重跑 license/security/MSRV/Linux/Windows/package/startup证据，确认实现没有偏离task 1.5 exact approval。
- [ ] 7.2 用 `bun --silent run dnm outline`/`read` 检查所有更新 Markdown结构与关键sections，并用局部diff确认 owner rule只完整写在对应主规范，OpenSpec/predecessor不被误标为Current。
- [ ] 7.3 运行 `bun run verify:docnav-workspace`，检查CLI smoke、Rust tests、schemas、OpenSpec strict validation、test-evidence closure和release validation全部通过；无法执行项记录具体影响，不声称完成。
- [ ] 7.4 对代表性large directory/multi-format/slow single-document workload只记录可复现observation与owner attribution；不得从结果自动建立timing gate、fast-find、size skip、parallel worker、ranking、index或cache。
- [ ] 7.5 最终审查确认只有approved traversal dependency（或none）、automatic one-inference exact mapping、document-local ambiguity/unclassified routing failure、static registry conflict fatal、explicit manifest prefilter、single-document adapter dispatch、build-private positive finite quantum、numeric replay和path+opaque ref wrapper；确认public schema/examples无exact quantum，且无candidate-execution fallback、project-aware adapter operation、synthetic document path、opaque cursor、progress、persistent index/cache、ranking或auto-read。
