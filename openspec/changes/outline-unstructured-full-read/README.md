# outline-unstructured-full-read

navigation 解析 path batch selector 或 adapter-scoped cost threshold selector 后产出标准 `outline_mode = "unstructured_full"` 时，在 adapter 正常 outline dispatch 前直接返回非结构化全文内容，不再要求 ref 或分页。
