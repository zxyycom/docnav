# add-adapter-readable-view-rendering

Allow adapters to optionally provide custom readable-view text renderers, and let the Markdown adapter use that hook for md-like readable-view output while keeping the generic fallback.
