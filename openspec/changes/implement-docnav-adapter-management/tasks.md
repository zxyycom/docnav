一句话核心：实现正式 adapter 管理，让外部 adapter 的来源、manifest、协议兼容和 hash 都可审计。

## 0. 审计门禁

- [ ] 0.1 用户审计确认：用户已审计本 change 的 proposal、design、spec 和 tasks，并明确允许开始实现；未完成本项前，1.x 及后续任务全部处于阻塞状态。

## 1. 安装记录与列表

- [ ] 1.1 （阻塞：等待 0.1 用户审计确认）设计并实现 adapter 安装记录结构，保存 manifest 快照、来源、可执行入口和健康状态。
- [ ] 1.2 （阻塞：等待 0.1 用户审计确认）实现 `docnav adapter list`，展示身份、格式、协议范围、来源和可用状态。
- [ ] 1.3 （阻塞：等待 0.1 用户审计确认）处理安装记录缺失、损坏和 adapter 不可用状态。

## 2. Install

- [ ] 2.1 （阻塞：等待 0.1 用户审计确认）实现本地可执行文件来源解析、可执行性检查和绝对路径记录。
- [ ] 2.2 （阻塞：等待 0.1 用户审计确认）实现本地可执行文件 SHA-256 hash 计算和安装记录写入。
- [ ] 2.3 （阻塞：等待 0.1 用户审计确认）实现 GitHub 链接来源解析和制品信息记录。
- [ ] 2.4 （阻塞：等待 0.1 用户审计确认）安装时执行 manifest，校验 manifest schema 和协议兼容性。
- [ ] 2.5 （阻塞：等待 0.1 用户审计确认）确保安装校验失败时不注册 adapter。

## 3. Update 与 Remove

- [ ] 3.1 （阻塞：等待 0.1 用户审计确认）实现 `docnav adapter update [adapter-id]`，使用旧来源获取或重新验证候选版本。
- [ ] 3.2 （阻塞：等待 0.1 用户审计确认）确保 update 在 manifest、schema、协议和 hash 校验通过后才替换旧记录。
- [ ] 3.3 （阻塞：等待 0.1 用户审计确认）确保 update 失败时保留旧版本并返回结构化错误。
- [ ] 3.4 （阻塞：等待 0.1 用户审计确认）实现 `docnav adapter remove <adapter-id>`，清理管理记录。
- [ ] 3.5 （阻塞：等待 0.1 用户审计确认）处理仍被项目配置显式引用时的 remove 失败或 guidance。

## 4. 验证与审计

- [ ] 4.1 （阻塞：等待 0.1 用户审计确认）覆盖本地 exe install/list/update/remove 和 hash 失配测试。
- [ ] 4.2 （阻塞：等待 0.1 用户审计确认）覆盖 GitHub source 解析失败和 manifest 校验失败测试。
- [ ] 4.3 （阻塞：等待 0.1 用户审计确认）验证 core CLI 运行前健康检查不会调用 hash 失配 adapter。
- [ ] 4.4 （阻塞：等待 0.1 用户审计确认）用局部 diff 确认只修改 adapter 管理和相关测试范围。
