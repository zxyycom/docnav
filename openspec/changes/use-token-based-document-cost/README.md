# use-token-based-document-cost

为 adapter SDK cost/budget helper 和 Markdown token-informed cost 展示建立初始 change。

本 change 不单独决定 raw protocol `cost` 字段结构；具体字段 shape 等待 `explore-structured-protocol-fields` 收敛。
