This temporary OpenSpec task plan keeps JSONC implementation blocked until the selected grammar/identity/output/safety/diagnostic contract is verified, one parser implementation is approved from complete dependency evidence, and the routing predecessor is Current.

## 0. Blocking investigation, decisions, and artifact audit

Tasks 0.1–0.10 are investigation and planning work only. **No task in sections 1–6 may begin, and no Cargo manifest, lockfile, production code, owner doc, main spec, schema, example, fixture, Case ledger, test, or release artifact may be modified, until task 0.11 is explicitly completed.** Mechanical completion of OpenSpec `applyRequires` artifacts does not satisfy this gate and must not be described as apply-ready.

- [ ] 0.1 Reconfirm the baseline from `docs/navigation.md`, JSON/adapter/ref/protocol/output owners, the Current main `json-adapter` spec, Current code/tests/release evidence, and the final on-disk `replace-probe-traversal-with-inferred-routing` artifacts. Record in `design.md` the exact strict-JSON parser/model/operation invariants and any difference between owner Target wording and implementation evidence; do not edit those sources.
- [ ] 0.2 Build one grammar conformance corpus covering the selected `//` and non-nested `/* ... */` comments at every token boundary, EOF comments, LF/CRLF/CR, Unicode, comment markers and escapes inside strings, accepted single object/array trailing commas, and every rejected nested/unterminated comment, `#` comment, missing/multiple comma, multiple root/trailing input, single quote, unquoted name, hexadecimal/unary-plus number, leading/trailing decimal point, `NaN`/infinity, empty-container, and root-scalar case. Compare every viable candidate against the exact Decisions 1–2 oracle; do not let a parser default revise it.
- [ ] 0.3 Audit the one-strategy identity against the final routing owner: verify one normalized `json` descriptor, predecessor hints plus `.jsonc`, content-type ownership, case handling, extensionless/unknown/misleading paths, automatic routing, and explicit `--adapter docnav-json` all lead to the same grammar without a mode/dialect field. Record a one-way handoff that adds no parser semantics or reverse dependency to `replace-probe-traversal-with-inferred-routing`; do not edit that change.
- [ ] 0.4 Verify the selected structured/source output matrix: normalized strict-JSON `read` and `application/json`; comment/trailing-comma removal; member order, raw number, string escape, scalar and newline behavior; source `find` including comments; deepest-container/root trivia mapping; full-read BOM/source preservation; syntax-derived `application/json`/`application/jsonc`; format id `json`; raw protocol facts versus generic readable framing. Record schema/example impact and adversarial cases.
- [ ] 0.5 Audit the selected safety and diagnostics with malformed and hostile inputs: UTF-8 and optional/multiple BOM, complete input, strict/raw numbers, decoded duplicate members, depth `127/128`, node/region accounting, long comments/lines, large object/array/comment corpora, bounded auxiliary state, document changes, and parser failures. Verify the predecessor's exact `DOCUMENT_CONTENT_INVALID` JSON reasons plus existing encoding/path codes cover every failure with no mode conflict or parser type/message/confidence crossing the adapter boundary.
- [ ] 0.6 Compare the zero-new-dependency Current `serde_json` plus bounded adapter-private scanner/neutralization baseline with maintained purpose-built parser and preprocessing candidates, including at least the discovery seeds `serde_json_lenient`, `jsonc_parser`, and `json_strip_comments`. For each record exact version/features, API/grammar fit, raw-number/order/duplicate/depth/span behavior, source fidelity, ecosystem adoption/activity, advisories/unsafe/supply-chain risk, license/notice, MSRV/toolchain, target support, direct/transitive graph, compile cost, release executable/package-size delta, startup/operation measurements, rollback cost, and owned-code maintenance. Select exactly one implementation path and at most one new direct parser dependency; a larger crate remains eligible only when its minimal feature graph and measured cost are small enough. Run spikes only in a secure temporary workspace and do not modify or commit this workspace's Cargo files.
- [ ] 0.7 Use the same corpus, build profile, hosts/targets, commands, cache state, and repeat count for candidate comparisons. At minimum verify workspace Rust `1.96.0`, edition 2021, `x86_64-unknown-linux-gnu`, and `x86_64-pc-windows-msvc`, recording unavailable target evidence as a decision risk rather than assuming portability. Reject candidates whose unconfigurable grammar, source model, error behavior, or weight violates the approved contract.
- [ ] 0.8 **Parser/dependency approval gate:** an authorized approver must explicitly approve either the bounded zero-dependency implementation or one exact crate/version/minimal-feature set with accepted functional/security/license/target/graph/build/binary/package/startup/source-fidelity/rollback trade-offs. Agent recommendation, tests, benchmarks, crate size, or popularity cannot substitute for this approval.
- [ ] 0.9 After tasks 0.1–0.8, update `proposal.md`, `design.md`, `specs/json-adapter/spec.md`, and this task list with the approved exact parser implementation, measured evidence, rejected alternatives/reasons, and then-Current routing wording. Do not weaken or make conditional the selected observable contract, and do not edit the predecessor change.
- [ ] 0.10 Run a bounded doubt-driven contract challenge against the one-grammar claim, accepted `.json` broadening, ref opacity, source offsets/trivia mapping, raw/readable output, syntax-derived content types, pagination/order, diagnostics, untrusted input, dependency defaults, migration/rollback, and cross-change ownership. Classify every finding as a contract gap, valid issue, accepted trade-off, or noise; update the artifacts for every substantive finding and stop the gate if any contract gap or valid issue remains unresolved.
- [ ] 0.11 **Blocking artifact audit:** verify that each artifact begins with the same one-strategy JSONC-capable goal and temporary role; proposal capability id exactly equals existing `json-adapter` and delta directory; proposal/design/spec/tasks agree on grammar, one `json` identity/hints, normalized/source reads, safety, diagnostics, one parser implementation, and one-way routing handoff; no artifact claims apply readiness without task 0.8 dependency evidence and predecessor readiness; `## Remaining Gates` contains no hidden product choice; no parser dependency is selected without complete weight evidence and approval; no stable owner rule is left only in planning prose; no file outside this change was modified by task 0; `openspec validate support-jsonc-in-json-adapter --type change --strict --no-interactive` passes; and every artifact is readable with `bun --silent run dnm outline`. **Only after all checks pass may task 0.11 be checked and sections 1–6 begin.**

## 1. Restore test evidence and establish failing behavior

- [ ] 1.1 Read `docs/coding-style.md`, `docs/testing.md`, the then-Current JSON/routing/output owners, `docs/testing/case-maintenance.md`, and the project `test-evidence-review` skill; run the project wrapper that proves the complete Current static entities, runner entities, and Case mapping are closed before changing tests.
- [ ] 1.2 Add or update semantic Cases for strict-document non-regression, one JSONC-capable grammar through pathname and explicit selection, source/normalized outputs, source-region/ref mapping, diagnostics, core/release behavior, and every renamed/split/merged test entity; rerun the Case completeness wrapper before product test edits.
- [ ] 1.3 Add failing loader/model tests for all approved positive and negative grammar cases, UTF-8/BOM, complete input, decoded duplicates, strict/raw numbers, depth `127/128`, order, node counts, source regions, and hostile bounded-work cases.
- [ ] 1.4 Add failing operation tests proving outline/ref/read equivalence for logically equal strict/JSONC documents, normalized structured read, comment/source find mapping, info/full-read facts, pagination, cost, root scalars, empty containers, and raw/readable separation.
- [ ] 1.5 Add failing core/CLI/release tests for one `json` identity, `.json`/`.jsonc`/`.code-workspace` and exact-filename hints, automatic and explicit use of the same grammar, selected invalid JSON without adapter fallback, closed public inputs, syntax-derived source content type, packaged binary behavior, and affected schema/example facts.

## 2. Synchronize owner contracts and validation materials

- [ ] 2.1 Update `docs/adapters/json.md` and the main `openspec/specs/json-adapter/spec.md` with the one JSONC-capable grammar, identity, private model, ref/navigation, source/structured output, safety, diagnostics, and verification contract; replace the Current statement that comments/trailing commas are out of scope without implying JSON5 support.
- [ ] 2.2 Update adapter/routing/output/protocol/diagnostic owner docs only where task 0 proved an observable cross-owner fact changes; keep routing limited to identity/hints and keep JSONC grammar/source semantics in the JSON owner.
- [ ] 2.3 Update manifest/protocol schemas, examples, fixtures, generated or checked validation material, and release inspection expectations only for approved observable format-id/content-type/diagnostic changes; do not add a second schema source or readable-only field to raw protocol.
- [ ] 2.4 Run owner-specific Markdown, schema, example, fixture, and contract-validation checks, then inspect the local diff to prove all synchronized materials express the same approved matrix.

## 3. Implement the approved parser and private model

- [ ] 3.1 Apply only the task-0-approved dependency/version/features, if any, to the workspace and JSON adapter manifests; update the lockfile once, inspect the resolved graph against the approved evidence, and stop if resolution changes the accepted license/security/weight facts.
- [ ] 3.2 Implement one deterministic JSONC-capable parse path for every selected document without a caller option, pathname classifier, format-id argument, parse-failure retry, routing callback, or parser state crossing the adapter contract.
- [ ] 3.3 Implement the exact comments/trailing-comma grammar while preserving strict JSON document semantics; explicitly disable every broader leniency control and normalize dependency errors into private loader categories.
- [ ] 3.4 Build the shared ordered source-aware logical model with approved comment/token auxiliary state, original source offsets, raw number tokens, decoded duplicate rejection, root-at-zero depth limit, bounded work/memory, and no second full logical tree.
- [ ] 3.5 Implement controlled drop/error behavior for maximum-depth and hostile inputs, then make every parser/model test from section 1 pass without weakening strict cases.

## 4. Extend JSON operations without changing refs or wrappers

- [ ] 4.1 Reuse the existing canonical `json:#` generator/parser and deterministic preorder traversal for JSONC logical nodes; prove comments/trailing commas never create refs, entries, node kinds, or pagination drift.
- [ ] 4.2 Implement the approved normalized structured-read serializer/content type while preserving source order, raw number tokens, pinned scalar spelling, two-space containers, full-selection cost, Unicode pagination, input ref, and existing response shape.
- [ ] 4.3 Implement approved source-region/trivia ownership and JSONC `find` behavior with original-source occurrences, bounded labels/work, deterministic ref mapping, line locations, occurrence order, and paginated forward progress.
- [ ] 4.4 Implement approved JSONC info and unstructured full-read format/content-type/source/BOM/cost facts without adding entries, refs, pages, continuation, or readable framing to the full-read hook.
- [ ] 4.5 Map every JSON/JSONC load/semantic/operation failure to `DOCUMENT_CONTENT_INVALID`, the existing encoding/path code, or another owner-approved diagnostic as specified; prove no parser internals, parse retry, routing retry, or adapter fallback occurs.

## 5. Targeted and cross-layer verification

- [ ] 5.1 Run formatting plus targeted JSON adapter unit/integration tests and strict/JSONC corpus tests; confirm strict JSON document snapshots remain unchanged while comments/trailing commas become accepted and every broader extension stays rejected.
- [ ] 5.2 Run core/navigation/CLI tests for manifest inspection, automatic and explicit routing, closed inputs, selected-failure no-fallback behavior, protocol-json, generic readable-view, pagination, cost, info/full-read, and diagnostics.
- [ ] 5.3 Run schema/example/fixture validators, the test-evidence completeness wrapper, and release-package smoke for Linux and Windows evidence required by the approved dependency/identity matrix.
- [ ] 5.4 Run `bun run verify:docnav-workspace`; investigate every failure rather than narrowing the command until it passes or an external limitation is recorded with impact.

## 6. Final review and archive readiness

- [ ] 6.1 Review the complete diff for owner boundaries, accidental public inputs, parser-default leakage, stale probe/routing wording, source/normalized content-type mismatches, dependency graph drift, and unrelated workspace changes; preserve all pre-existing user work.
- [ ] 6.2 Re-run strict OpenSpec validation and `dnm outline` for every changed Markdown file, record final verification evidence and remaining limitations, and confirm owner docs/main specs/code/tests/release artifacts describe Current versus Target state consistently.
- [ ] 6.3 Complete an independent findings-first review of the approved high-risk claims (strict-document stability, accepted `.json` broadening, source offsets/trivia-to-ref mapping, raw normalization, content types, diagnostics, hostile input, and dependency weight); resolve all actionable findings before requesting archive.
