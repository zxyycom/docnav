# support-jsonc-in-json-adapter

Extend the built-in JSON adapter with one controlled JSONC-capable grammar while preserving strict-JSON document semantics, auditable source mapping, and normalized structured output.
