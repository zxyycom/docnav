**This checklist keeps all contract and implementation work blocked until a human owner selects and records one complete find result model and work budget.**

**Artifact state:** 0/32 tasks are complete. The executable order is task 1.1 human approval → task 1.2 persistence of one exact Target → task 1.3 blocking artifact audit → tasks 2.1–7.5. Artifact-ready does not bypass any gate, and an agent may not substitute its review for task 1.1 approval.

## 1. Human decision and blocking artifact audit

- [ ] 1.1 **Human decision gate: tasks 1.2–7.5 are blocked until the user or designated product/architecture owner explicitly approves the complete decision packet.** Select occurrence, distinct exact-ref/node, or grouped logical results; `Entry` versus a dedicated/versioned wire type; the top-level field; and, for each of `ref`, `label`, `kind`, `location`, `summary`, `excerpt`, `rank`, `cost`, and `metadata`, its preserve/delete/replace disposition, precise meaning, requiredness, and compatibility treatment (or an explicit statement that it retains its complete Current contract). Also select multiplicity completeness; deterministic ordering; final page unit and continuation; current-page versus query-global auto-read; monotonic traversal/replay/seen-set/lookahead proof for source-order occurrence or first-occurrence distinct-ref pages; full-scan/index proof only for selected query-global uniqueness, exact-total, complete-grouping, or all-candidate global-rank facts; first/subsequent-page scan budget; retained ref/group/evidence/spill budget; budget-exhaustion behavior; and compatibility/version/rollback path. Compare Markdown, the post-archive JSON follow-up scenario, code, and large state/config scenarios. The `cost` wire decision MUST leave estimator/calculator mechanics to the independent token-cost change. Agents MUST NOT infer approval from Current behavior, implementation convenience, benchmarks, or a technically efficient candidate.
- [ ] 1.2 Record every approved answer as new numbered Decisions in `design.md`, remove the corresponding Open Questions, and rewrite the provisional protocol/output/navigation/Markdown deltas into one exact implementable contract. If query-global eligibility makes `unique-ref eligibility uses refs in the current returned result` inaccurate, use the OpenSpec `RENAMED` operation rather than archiving a misleading requirement name. If any answer remains open or the selected model has incompatible field/work assumptions, keep the change implementation-blocked.
- [ ] 1.3 **Blocking artifact audit: tasks 2.1–7.5 MUST NOT begin until this audit passes.** Verify proposal, design, specs, and tasks share the opening core goal; capability IDs match existing main owners; all provisional-delta banners and approval-dependent alternatives have been replaced by the selected exact Target contract; all nine Current `Entry` fields have an explicit disposition; no temporary artifact is described as already shipped; no existing owner doc, main spec, schema/example, implementation, other change, or active decision was modified while drafting; the task-complete but unarchived `add-json-adapter` remains untouched and only a post-archive handoff is recorded here; and `## Open Questions` contains no unanswered or ambiguously “resolved” decision. Record who approved the packet and the audit evidence without marking agent review as product approval.

## 2. Owner contract, schema, examples, and compatibility

- [ ] 2.1 Update the owner docs for protocol find facts/page, readable find presentation, navigation auto-read selection, and Markdown find production first. Update architecture, adapter-contract, or ref owner docs only if the approved model actually changes those boundaries; keep adapter query/source-to-ref/order/ref interpretation, navigation composition, protocol facts, output presentation, and opaque-ref ownership explicit.
- [ ] 2.2 Update the shared protocol schema and schema descriptions for the approved `FindResult`, item/group type, the approved disposition and requiredness of every Current field (`ref`, `label`, `kind`, `location`, `summary`, `excerpt`, `rank`, `cost`, `metadata`), multiplicity/completeness, continuation, and `auto_read` combination. Reject invalid model/type pairs, partial facts presented as complete, unknown group fields, and incompatible continuation combinations. Do not select or implement a cost estimator in this change.
- [ ] 2.3 Update protocol-json and readable-view contract examples for zero/one/many logical units, repeated occurrences mapping to one ref, multiple refs, pagination, truncated evidence/group state, eligible/ineligible auto-read, and nested read continuation. Examples MUST use the same immutable response facts without renderer-owned grouping or identity.
- [ ] 2.4 Inventory Rust and serialized consumers of `matches: Entry[]`, every Current `Entry` field and its requiredness, occurrence order, repeated refs, integer `page`, and current-page auto-read. Implement the approved additive, bounded dual-transition, or breaking version/migration plan in contract materials, including deprecation window, release notes, old/new source of truth, rollback boundary, and fixture compatibility.
- [ ] 2.5 Record in this change the post-archive `add-json-adapter` handoff for the approved raw logical unit, evidence, source order/node order rule, exact-ref grouping, pagination/continuation, work bounds, and auto-read completeness proof. `add-json-adapter` is task-complete but unarchived; do not edit or rebase it. Record a separate `add-json-readable-renderer` handoff that consumes finalized raw facts but leaves renderer implementation and presentation decisions in that change.

## 3. Evidence gate

- [ ] 3.1 Before adding, deleting, splitting, or renaming tests, read `docs/testing.md`, the approved behavior owners, `docs/testing/case-maintenance.md`, and the project `test-evidence-review` skill; run the project wrapper that proves the complete Current tree's static entities, runner entities, and Case mapping are closed. Stop and repair/report that baseline according to the owner workflow before changing test evidence.
- [ ] 3.2 Preserve independent Current evidence and add/update Case records for zero matches, one occurrence/ref, repeated occurrences sharing one ref, multiple refs, interleaved `A/B/A` occurrences, page beyond end, and one heavily matched ref preceding another ref on a later page.
- [ ] 3.3 Add failing contract/schema/Rust evidence for the approved item or group type; the preserve/delete/replace, meaning, requiredness, and compatibility decision for each of `ref`, `label`, `kind`, `location`, `summary`, `excerpt`, `rank`, `cost`, and `metadata`; exact versus partial multiplicity; representative/nested evidence; deterministic ordering; Unicode/whitespace truncation; oversized logical units; page/continuation round trips; and invalid mixed-model payloads.
- [ ] 3.4 Add failing navigation evidence for the approved current-page or query-global exact-ref scope, incomplete-result suppression, authoritative complete-index/exhaustive proof only when query-global uniqueness is selected, non-null base continuation, page greater than one, nested-read success/failure/invalid composition, and unchanged base response on every non-composed outcome. Do not require a complete-query proof merely for source-order occurrence or first-occurrence distinct-ref paging.
- [ ] 3.5 Add measurable large-document evidence for first-page and subsequent-page scan limits; deterministic replay; seen-ref and retained group/evidence/counter/offset/spill limits; duplicate-skipping lookahead; selected exact-total/query-global-uniqueness/complete-group/all-candidate-rank proof; budget exhaustion; and cleanup/failure behavior. The evidence MUST observe current-page scan and retained work, not infer bounded work from a small output page or treat every ordering/dedup page as requiring exhaustive proof.
- [ ] 3.6 Add Markdown evidence for source occurrence/ref mapping, repeated heading refs, `HEAD:leading`, `doc:full` fallback, ref-to-read round trips, source/approved ordering, long groups or distinct-node representatives, and continuation without loss or duplication. Re-run Case/static/runner closure before implementation begins.

## 4. Shared protocol, adapter contract, and navigation

- [ ] 4.1 Implement the approved shared protocol result/item/group types, field validation, completeness rules, operation/result pairing, serialization/deserialization, schema parity, and base-plus-`auto_read` validation. Preserve complete opaque refs and reject invalid logical-unit/continuation combinations.
- [ ] 4.2 Change the linked adapter result contract only as required by the approved model. Keep query and format-private state out of protocol, avoid an adapter-specific implicit shape, and provide the approved compatibility bridge for existing linked producers/consumers.
- [ ] 4.3 Implement navigation ref extraction from the finalized logical units and the approved current-page or query-global completeness facts. Use string-exact non-empty opaque refs, do not parse/normalize refs or rebuild grouping, invoke nested read at most once from page 1, and preserve the validated base response on ineligible or unsuccessful composition.
- [ ] 4.4 Implement only the minimum private accumulator, lookahead, index view, continuation bookkeeping, or spill mechanism required by the approved logical unit and work budget. Do not add a shared producer/sink/grouping abstraction unless at least two real consumers demonstrate the same lifecycle, errors, budgets, and continuation.
- [ ] 4.5 Prove shared validation and navigation tests from section 3 now pass, including protocol-json behavior, exact compatibility/version behavior, auto-read scope, partial/global proof rejection, and unchanged outline auto-read semantics.

## 5. Markdown adapter

- [ ] 5.1 Implement Markdown production of the approved occurrence, distinct-ref/node, or grouped logical units while preserving literal source search, deterministic source-to-ref mapping, complete refs, `HEAD:leading`, `doc:full`, and ordinary read round trips.
- [ ] 5.2 Implement the approved per-field disposition for `ref`, `label`, `kind`, `location`, `summary`, `excerpt`, `rank`, `cost`, and `metadata`, plus representative evidence, nested occurrences, multiplicity, and completeness. Apply deterministic Unicode-safe compaction/truncation and preserve the finalized minimum non-empty facts when one logical unit exceeds the output budget. Consume the existing/finalized cost contract only; do not implement estimator mechanics from the independent token-cost change.
- [ ] 5.3 Implement final-logical-unit ordering, page boundaries, continuation lookahead/replay, page-beyond-end behavior, and any partial-group continuation. Verify interleaved refs and later duplicates neither disappear nor reappear contrary to the approved dedup/group scope.
- [ ] 5.4 Enforce the approved current-page scan and retained-work bounds for first and subsequent pages, including monotonic traversal, replay, seen-ref state, duplicate-skipping lookahead, and budget exhaustion. Require full traversal or an authoritative complete index only for selected query-global uniqueness, exact-total, complete-grouping, or all-candidate global-rank facts. Count and clean up adapter-private indexing or spill work according to the approved rule.
- [ ] 5.5 Run Markdown unit/integration evidence for repeated refs, cross-page results, document head/full fallback, Unicode/long evidence, work limits, exact/partial facts, and find-to-read round trips.

## 6. Readable output

- [ ] 6.1 Update the built-in find projection for the finalized raw item/group facts, evidence, multiplicity/completeness, truncation, page/continuation, and optional `auto_read`. Keep display derived, refs opaque, and grouping/representative selection outside the renderer.
- [ ] 6.2 Preserve one immutable `ProtocolResponse` for protocol-json and readable-view; prove the renderer performs no source scan, ref parsing, dedup/grouping, hidden read, later-page fetch, uniqueness recalculation, or fallback render attempt.
- [ ] 6.3 Update readable fixtures/goldens for all approved logical-unit states and verify nested `/auto_read/read/content` framing, byte lengths, empty-stdout render failure, and unchanged outline/unstructured/read/info projections.

## 7. Integration, handoffs, and closure

- [ ] 7.1 Add end-to-end CLI/smoke coverage that compares protocol-json and readable-view for zero/one/many units, repeated refs, continuation round trips, partial/complete evidence, current-page/query-global auto-read, nested read continuation/failure, and page-beyond-end behavior.
- [ ] 7.2 Execute the recorded JSON handoff only after the task-complete `add-json-adapter` change is archived and a main capability owner exists. Until then, keep any incompatibility recorded in this change for post-archive follow-up; do not edit, rebase, or add tasks to `add-json-adapter`, and do not edit or rebase `add-json-readable-renderer` from this task.
- [ ] 7.3 Notify/handoff affected MCP, skim-pack, interactive-outline, and future code-adapter consumers through their own owner changes. Do not implement unrelated consumer features here, and do not make approximate token-cost or document-state-reuse changes prerequisites.
- [ ] 7.4 Run schema/example validation, targeted Rust format/lint/tests, Case closure checks, deterministic pagination and work-bound checks, `bun run verify:docnav-workspace`, release-package smoke/public validation, strict OpenSpec validation, and a change-local diff review.
- [ ] 7.5 Complete a final bounded adversarial review of wire compatibility, ref opacity, logical-unit completeness, cross-page ordering/dedup/grouping, auto-read proof, Unicode truncation, hidden whole-input work, resource cleanup, output parity, rollback, and active-change handoffs. Resolve every valid issue, confirm owner docs/specs/schema/examples/code/tests agree, then mark the implementation tasks complete.
