# adopt-core-linked-adapter-libraries

Make default document operations use adapter-layer workspace crates shipped in the `docnav` core release, registered through one static core registry, while preserving adapter-owned parsing, ref, navigation, pagination, and native option boundaries.
