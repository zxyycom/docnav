本 tasks 清单是 `replace-probe-traversal-with-inferred-routing` 的临时执行工件：它把依赖、probe 兼容性和 cross-change 人工 gate 放在所有测试与实现之前，当前不授权任何 production 或 dependency 修改。

## 0. Blocking audit and explicit approval

Tasks 0.1–0.11 are planning/audit work only. **No task in sections 1–7 may begin, and no Cargo manifest, lockfile, production code, owner doc, main spec, schema, example, fixture, test, or release artifact may be modified, until task 0.11 is explicitly completed.**

- [ ] 0.1 Create change-local `dependency-audit.md`; anchor its first sentence to this change and record the Current source-backed routing/probe call chain, registry/manifest ownership, all production/test/docs/schema/release consumers, and the exact automatic/explicit diagnostic baseline without modifying those surfaces.
- [ ] 0.2 Compare at least one mature existing Rust format-inference library, credible alternatives, and the no-new-dependency baseline; for each record ecosystem adoption, maintenance/release/issues, advisories/unsafe/transitive supply-chain risk, license/notice compatibility, Rust edition/toolchain/MSRV evidence, features, dependency count, and upgrade/rollback cost.
- [ ] 0.3 Run the same bounded JSON/Markdown inference corpus for every candidate: normal and uppercase extension, extensionless, empty, BOM, non-UTF-8, misleading extension, content/extension conflict, malformed, ambiguous/polyglot, and representative large inputs; record false positive/negative, failure taxonomy, normalization mapping, future code-format fit, and prove raw library enum/text/confidence/errors can stay private.
- [ ] 0.4 Build candidate spikes outside production integration for `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-msvc`; record exact commands and results, incremental binary/package size, clean/cold/warm startup, build/host/cache/repeat facts, and accepted measurement limitations. Do not commit Cargo or lockfile changes as part of the spike.
- [ ] 0.5 Complete the probe compatibility inventory across Rust exports/re-exports, CLI inspection, protocol constants/types/decoders, runtime contract validation, typed-field consumer definitions, schemas/examples, scripts, package/release materials, tests, docs, and known external promises. Record evidence-backed complete deletion of `Adapter::probe`, `AdapterDefinition::probe`, probe protocol/decode/validation/typed-field/schema/fixture surface, and probe-only candidate vocabulary. If a real owner-backed consumer exists, stop current apply and return to artifacts/human approval; this task cannot be completed by retaining an inspection surface.
- [ ] 0.6 **`add-project-wide-find` predecessor planning-handoff audit:** record in this change that project Decisions 5/12 already provide artifact-level acceptance of this routing predecessor and its filter/local/fatal seam. Verify the two artifacts agree, but do not require project task 1.3, implementation or validation to complete; those remain downstream of this change's final implementation/validation. Do not edit the project change from this task.
- [ ] 0.7 **`reuse-adapter-document-state` downstream handoff:** record that final no-probe routing becomes that change's future Current selection baseline, while its selected/full-read/nested-read/snapshot/cleanup/private-state objective remains independently undecided. Do not require its mechanism gate or implementation to complete and do not edit that change. If a source-backed audit shows no-probe would break its core goal rather than only invalidate old candidate-traversal wording, stop task 0.11 for human resolution.
- [ ] 0.8 **`add-ast-grep-code-adapter` downstream handoff:** record that the code-adapter change must replace its old probe/registry-order baseline with final no-probe/exact routing before dependency or production work. Do not preselect its dependency, language coverage or parser implementation, do not require its implementation to complete, and do not edit that change. Stop task 0.11 only if the handoff audit finds a core-goal incompatibility requiring human resolution.
- [ ] 0.9 **Human dependency and compatibility decision gate:** obtain explicit user or designated architecture/product-owner approval for the exact crate/version requirement/features, normalization mapping, accepted target/coverage/size/startup/license/security trade-offs, complete probe deletion, exact diagnostic table, and registry duplicate release/runtime policy. A no-dependency decision means no implementation under this design and return to artifacts/closure, not permission to write a custom detector. Agents, audit results, benchmarks, or implementation convenience cannot close this task.
- [ ] 0.10 After tasks 0.5–0.9, update proposal, design, all nine affected capability deltas, tasks, and `dependency-audit.md` with approved facts, rejected alternatives, and the three cross-change handoff records; close every `## Open Questions`, remove conditional placeholders, and keep probe deletion complete rather than adding an inspection fallback.
- [ ] 0.11 **Blocking artifact audit:** verify every artifact's first sentence states this goal and its temporary role; proposal capability ids exactly match complete delta directories and existing main-spec ids; every `MODIFIED` block contains the complete current requirement plus target edits; every removal has reason and migration; exact outcome→code→details/project classification agrees across architecture, navigation, diagnostics, protocol, project handoff and tasks; `candidate_failures`, probe stage/reasons and third-party facts are absent from the Target public contract; registry duplicate is release-blocking/global-internal rather than document ambiguity; complete probe method/type/decode/validator/typed-field/schema/evidence deletion is explicit; project predecessor planning acceptance and the state-reuse/code-adapter downstream rebase handoffs are recorded, with any proven core-goal incompatibility human-resolved; dependency choice is approved; no artifact claims unapproved/apply-ready behavior; `## Open Questions` has no unanswered or wording-hidden choice; only this change directory was written by proposal work while unrelated worktree changes remain preserved; `openspec status --change replace-probe-traversal-with-inferred-routing --json` reports all `applyRequires` done; and `openspec validate replace-probe-traversal-with-inferred-routing --strict` passes. **Sections 1–7 remain blocked until this checkbox is deliberately checked.**

## 1. Restore test evidence and establish failing proof

- [ ] 1.1 Read `docs/testing.md`, the nine affected capability owners, `docs/testing/case-maintenance.md`, and the project `test-evidence-review` skill in that order; run the project wrapper that proves complete current-tree static entities, runner entities, and Case mappings are closed before modifying tests.
- [ ] 1.2 Add or update semantic Cases and focused failing tests for exactly one automatic inference call, project-owned normalization, exact unique format match, registry-order independence, `FORMAT_UNKNOWN` unknown/unsupported exact details, `FORMAT_AMBIGUOUS` for every multiple-identity outcome including empty/single mapped candidate lists, registry-conflict `INTERNAL_ERROR`, and absence of adapter probe traversal/candidate failures.
- [ ] 1.3 Add or update failing tests for explicit exact adapter-id lookup bypassing inference, missing declared adapter diagnostics, real selected-adapter parse, and parse/semantic/operation/invalid-result failures never dispatching another adapter.
- [ ] 1.4 Add Markdown and JSON owner evidence for removal of selection probe, selected operation parsing, JSON depth/duplicate/member/TOCTOU behavior, and replacement of `json-document-changed-after-probe` with normal owner diagnostics.
- [ ] 1.5 Add core and release smoke evidence for automatic Markdown/JSON selection, explicit selection, outline-to-ref-to-read, single binary delivery, project filter/local/fatal classification handoff, and no raw inference-library or removed probe fields in protocol/readable/log output.

## 2. Synchronize owner contracts and validation materials

- [ ] 2.1 Update `docs/adapter-contract.md`, `docs/navigation-input-resolution.md`, `docs/architecture.md`, `docs/protocol.md`, and `docs/navigation.md` owner summaries with the approved private inference → normalized identity → validated exact registry match pipeline, component ownership, explicit override, exact diagnostic table, no fallback, and complete probe deletion.
- [ ] 2.2 Update `docs/adapters/markdown.md` and `docs/adapters/json.md`; preserve adapter-owned parse/ref/operation semantics, remove selection-probe obligations, and migrate JSON post-probe mutation semantics to the selected operation's normal document/JSON diagnostic.
- [ ] 2.3 Delete probe protocol constants/types/re-exports/decoders, runtime contract validator, typed-field consumer definitions, `docs/schemas/probe-result.schema.json`, examples, fixtures, schema indexes, candidate-failure/probe vocabulary, and conformance/release references after task 0.5 proves no real owner; if task 0.5 finds one, stop rather than retaining inspection-only code.
- [ ] 2.4 Update testing strategy, current semantic Case ledgers, coverage mappings, and release verification documentation for the changed Current entities and observable behavior; do not use archived Cases or changes to invent unrelated coverage obligations.

## 3. Land only the approved dependency

- [ ] 3.1 Add only the task 0.9-approved crate version requirement and minimal features to the navigation-owned Cargo surface; update the lockfile and required license/notice materials, and verify the resolved transitive graph matches `dependency-audit.md`.
- [ ] 3.2 Implement a private direct library call and finite Docnav-owned normalization mapping without a custom inference trait, service object, plugin hook, scoring/confidence framework, generic detector registry, or automatic exposure of new upstream formats.
- [ ] 3.3 Implement the approved exact diagnostic table: distinguish `FORMAT_NOT_RECOGNIZED` from `NO_SUPPORTED_ADAPTER` + normalized `format`, emit sorted `FORMAT_MATCH` candidates for `FORMAT_AMBIGUOUS`, map registry conflict to `registry-format-identity-conflict`, preserve selected adapter diagnostics, and exclude raw enum/debug/message/confidence/detection evidence.

## 4. Replace routing and fallback behavior

- [ ] 4.1 Replace registry-order candidate traversal with exactly one automatic inference invocation followed by project-owned normalized identity and exact lookup in a manifest-derived registry format index.
- [ ] 4.2 Make registry construction/doctor/release validation reject duplicate normalized format identities; retain defensive runtime global `registry-format-identity-conflict`; make every multiple-normalized-identity inference outcome return `FORMAT_AMBIGUOUS` with only exact-mapped candidates; prove empty/single candidate lists and registry order never choose a winner.
- [ ] 4.3 Change explicit adapter selection to exact adapter-id lookup that bypasses inference and proceeds through the existing selected-operation resolution and closed input construction.
- [ ] 4.4 Remove candidate failure accumulation and probe-stage routing state; update diagnostic canonical payloads and protocol schema/examples so Target details omit `candidate_failures`, probe stages/reasons, and third-party evidence.
- [ ] 4.5 Preserve the selected definition through dispatch and prove selected parse, semantic, operation, invalid-result, full-read, and nested behavior never re-enter inference or dispatch another adapter.

## 5. Complete probe and adapter-owner migration

- [ ] 5.1 Remove `Adapter::probe`, `AdapterDefinition::probe`, selection-only `CandidateEvidence`/`CandidateStage`, probe version/reason/result types and all dead exports; do not retain an inspection-only surface.
- [ ] 5.2 Remove Markdown and JSON routing-probe implementations and migrate their owner tests to manifest routing plus selected operation parsing without changing ref, outline/read/find/info, pagination, cost, or full-read semantics.
- [ ] 5.3 Remove probe decode/runtime contract validation, probe typed-field `FieldDefSet`s, schema/examples/fixtures/indexes and validation tests; prove no current protocol, adapter, CLI, release or conformance surface references probe.
- [ ] 5.4 Verify implementation matches the accepted `add-project-wide-find`, `reuse-adapter-document-state`, and `add-ast-grep-code-adapter` handoffs from tasks 0.6–0.8; do not implement, rebase, or edit those changes under this task.

## 6. Targeted and workspace verification

- [ ] 6.1 Run formatting, targeted architecture/adapter-contract/navigation/diagnostics/protocol/contract-validation/typed-fields/Markdown/JSON unit and integration tests, core CLI tests, invocation-log tests, registry/doctor validation, and release smoke for every changed branch.
- [ ] 6.2 Run schema/example/fixture validation and searches proving all probe artifacts and candidate-failure/probe vocabulary are gone, exact routing details match schema/examples, and no upstream inference value leaks into public output.
- [ ] 6.3 Build and test the supported Linux GNU and Windows MSVC targets through the project-supported local/CI path; run canonical package verification and Markdown/JSON automatic plus explicit selection smoke from the manifest-resolved binary.
- [ ] 6.4 Repeat the accepted dependency graph, binary/package-size, startup, and representative routing measurements with the final implementation; compare against task 0 evidence and return to human review if an approved bound or assumption changed.
- [ ] 6.5 Run `bun run verify:docnav-workspace` and any owner-declared unified release validation required by the changed Rust/docs/schema/example/test surfaces.
- [ ] 6.6 Run `openspec validate replace-probe-traversal-with-inferred-routing --strict`, `dnm outline` for every change artifact and changed owner document, `git diff --check`, and a scoped final diff confirming unrelated worktree content was preserved.

## 7. Final review and archive readiness

- [ ] 7.1 Review the final diff against the approved dependency, normalization table, probe outcome, no-fallback semantics, minimal-implementation constraints, and all cross-change handoffs; reject any custom framework, second metadata owner, registry-order behavior, or unapproved public contract.
- [ ] 7.2 Obtain final architecture/product-owner acceptance of verification and risk evidence, confirm main specs/docs/code/tests/release artifacts describe the same Current behavior, and only then assess this change for archive.
