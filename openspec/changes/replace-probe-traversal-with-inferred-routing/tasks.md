本 tasks 清单定义 `replace-probe-traversal-with-inferred-routing` 的实施与验收顺序：先完成 probe removal inventory、cross-change handoff 和 artifact audit，再以测试证据驱动 owner 同步、实现、验证与最终评审。Tasks 0.1–0.11 已完成；sections 1–7 尚未实施。

## 0. Pre-implementation audit and explicit approval (complete)

Tasks 0.1–0.11 是 planning/audit work，现已全部完成。Sections 1–7 已解除 planning gate，但仍须按既定顺序执行；本状态不表示 Cargo manifest、lockfile、production code、owner doc、main spec、schema、example、fixture、test 或 release artifact 已经迁移。

Current execution state：实施前审计和人类决策门禁已关闭；下一步从 section 1 恢复测试 owner 与失败证据。Sections 1–7 当前均未完成。

- [x] 0.1 Consume and verify the formal investigation topic `docs/investigations/dependencies/format-routing-inference.md`; its complete report records the Current source-backed routing/probe call chain, registry/manifest ownership, all production/test/docs/schema/release consumers, and the exact automatic/explicit diagnostic baseline without treating the report as a change-owned artifact or modifying those surfaces.
- [x] 0.2 Compare at least one mature existing Rust format-inference library, credible alternatives, and the no-new-dependency baseline; for each record ecosystem adoption and available popularity signals such as reverse dependencies/downloads/credible production use, maintenance/release/issues, advisories/unsafe/transitive supply-chain risk, license/notice compatibility, Rust edition/toolchain/MSRV evidence, default/minimal features, direct/transitive dependency count, compile/binary/package weight, and upgrade/rollback cost.
- [x] 0.3 Run the same bounded JSON/Markdown inference corpus for every candidate: normal and uppercase extension, extensionless, empty, BOM, non-UTF-8, misleading extension, content/extension conflict, malformed, ambiguous/polyglot, and representative large inputs; record false positive/negative, failure taxonomy, normalization mapping, future code-format fit, and prove raw library enum/text/confidence/errors can stay private.
- [x] 0.4 Close the mechanism/measurement audit after the owner selected manifest-native zero-dependency routing: record the ordinary-extension corpus, `.prettierrc`/`.code-workspace` alias spike, `mime_guess` minimal resolved graph/binary/startup evidence and its alias gaps, the Current manifest/schema facts, and the accepted Windows/real-package limitations. Record that no Cargo/lockfile/license change is authorized and final cross-target/package verification remains in tasks 6.3–6.4.
- [x] 0.5 Complete the breaking probe removal inventory across Rust exports/re-exports, CLI inspection, protocol constants/types/decoders, runtime contract validation, typed-field consumer definitions, schemas/examples, scripts, package/release materials, tests, docs, and known external promises. Record evidence-backed complete deletion of `Adapter::probe`, `AdapterDefinition::probe`, probe protocol/decode/validation/typed-field/schema/fixture surface, probe-only candidate vocabulary, and `json-document-changed-after-probe`; migrate selected JSON syntax/trailing/duplicate/depth failures to the approved `DOCUMENT_CONTENT_INVALID` mapping. Every discovered consumer becomes an in-scope deletion, migration, or explicit breaking-impact record; discovery does not authorize an inspection/compatibility surface or stop apply on backward-compatibility grounds.
- [x] 0.6 **`add-project-wide-find` predecessor planning-handoff audit:** record in this change that project Decisions 5/12 already provide artifact-level acceptance of this routing predecessor and its filter/local/fatal seam. Verify the two artifacts agree, but do not require project task 1.3, implementation or validation to complete; those remain downstream of this change's final implementation/validation. Do not edit the project change from this task.
- [x] 0.7 **`reuse-adapter-document-state` downstream handoff:** record that final no-probe routing becomes that change's future Current selection baseline, while its selected/full-read/nested-read/snapshot/cleanup/private-state objective remains independently undecided. Do not require its mechanism gate or implementation to complete and do not edit that change. If a source-backed audit shows no-probe would break its core goal rather than only invalidate old candidate-traversal wording, stop task 0.11 for human resolution.
- [x] 0.8 **`add-ast-grep-code-adapter` downstream handoff:** record that the code-adapter change must replace its old probe/registry-order baseline with final no-probe/exact routing before dependency or production work. Do not preselect its dependency, language coverage or parser implementation, do not require its implementation to complete, and do not edit that change. Stop task 0.11 only if the handoff audit finds a core-goal incompatibility requiring human resolution.
- [x] 0.9 **Human routing architecture and dependency decision gate:** record the user's approval of manifest-owned exact-filename plus complete-basename suffix hints, route-before-document-I/O sequencing, ASCII-normalized longest-suffix matching, zero new routing/regex dependency, hint-only/no-content-validation semantics, private matched-format state, explicit adapter force-selection, selected-failure/no-fallback behavior, complete probe deletion, exact no-match diagnostic, and registry conflict release/runtime policy. Record that `NO_SUPPORTED_ADAPTER + format` and `FORMAT_AMBIGUOUS` are not outcomes of the validated manifest-derived automatic route, and activate `docs/decisions/adapter-evolution/route-by-manifest-basename-hints.md` as `active + unaligned` until owner docs and implementation converge; archive the narrower terminal-extension predecessor through a `修订` relation.
- [x] 0.10 After tasks 0.5–0.9, finish synchronizing proposal, design, all ten affected capability deltas, and tasks with the completed removal inventory, approved facts, rejected alternatives, the formal investigation citation, and all cross-change handoff records including `support-jsonc-in-json-adapter`; keep `## Open Questions` closed and probe deletion complete. Do not rewrite an older formal report to mirror change state: materially new alias or format-family evidence is appended as a new complete report and the investigation index is synchronized.
- [x] 0.11 **Artifact readiness audit:** 以下检查已全部完成，sections 1–7 的 planning gate 已解除。
  - 每个 artifact 的首句说明职责、Target/Current 关系和实施就绪状态；没有 artifact 把未应用行为写成 Current；`## Open Questions` 不含未回答或被 wording 隐藏的选择。
  - Proposal capability ids 与全部 delta directories、既有 main-spec ids 精确一致；每个 `MODIFIED` block 包含完整 Current requirement 与 Target edits；每个 removal 都有 reason 和 migration。
  - Architecture、navigation、diagnostics、protocol、JSON owner、project handoff 与 tasks 的 outcome → code → details/project classification 一致；`DOCUMENT_CONTENT_INVALID` 的 exact JSON reasons 已闭合；`candidate_failures`、probe stage/reasons、`NO_SUPPORTED_ADAPTER + format` 和 routing `FORMAT_AMBIGUOUS` 不进入 Target automatic contract；registry format/path-hint conflicts保持 release-blocking/global-internal。
  - Probe method/type/decode/validator/typed-field/schema/evidence 的完整删除明确；project predecessor、state-reuse、code-adapter 与 JSONC downstream handoffs 均有记录；任何已证明的 core-goal incompatibility 已由人类处理。
  - Manifest-owned complete-basename suffixes/exact filenames、route-before-document-I/O sequencing、ASCII-normalized longest-suffix 规则、zero-dependency mechanism、filename precedence、private match state、explicit override、exact outcomes 和 initial alias set 已由 task 0.9 批准并回写 artifacts。
  - Change artifact edits 只在本 change directory；长期决策维护只写对应 decision records 和 derived index；正式调查维护只写 `docs/investigations/dependencies/format-routing-inference.md` 和 derived index；四个 downstream change 保持只读，无关 worktree changes 保持不变。
  - `openspec status --change replace-probe-traversal-with-inferred-routing --json` 报告全部 `applyRequires` done，且 `openspec validate replace-probe-traversal-with-inferred-routing --strict` 通过。

Gate evidence：probe removal、cross-change handoff 与 artifact integrity 的完整结果保存在 `design.md` 的 `Completed Pre-Implementation Audit`；decision/investigation index checks、OpenSpec status/strict validation、文档 outline 与 scoped diff checks 在勾选本项前重新通过。

## 1. Restore test evidence and establish failing proof

- [ ] 1.1 Read `docs/testing.md`, the ten affected capability owners, `docs/testing/case-maintenance.md`, and the project `test-evidence-review` skill in that order; run the project wrapper that proves complete current-tree static entities, runner entities, and Case mappings are closed before modifying tests.
- [ ] 1.2 Add or update semantic Cases and focused failing tests for route-before-target-document-I/O, no-match without metadata/open/canonicalize/read, exact-filename precedence, complete-basename ASCII case-normalized suffix matching, longest compound-suffix precedence, end anchoring, extensionless unknown, initial JSON/Markdown hints, registry-order independence, exact `FORMAT_UNKNOWN / FORMAT_NOT_RECOGNIZED` with routing pathname, release/runtime format/path-hint conflicts, selected `DOCUMENT_CONTENT_INVALID` JSON reasons, and absence of adapter probe traversal/candidate failures.
- [ ] 1.3 Add or update failing tests for explicit exact adapter-id lookup bypassing pathname routing, missing declared adapter diagnostics, real selected-adapter parse, and parse/semantic/operation/invalid-result failures never dispatching another adapter.
- [ ] 1.4 Add Markdown and JSON owner evidence for removal of selection probe, selected operation parsing, JSON depth/duplicate/member/document-change behavior, and replacement of `json-document-changed-after-probe` with exact `DOCUMENT_CONTENT_INVALID` reasons or the existing path/encoding diagnostic.
- [ ] 1.5 Add core and release smoke evidence for automatic Markdown/JSON selection, explicit selection, outline-to-ref-to-read, single binary delivery, project filter/local/fatal classification handoff, and no private pathname/index or removed probe fields in protocol/readable/log output.

## 2. Synchronize owner contracts and validation materials

- [ ] 2.1 Update `docs/adapter-contract.md`, `docs/navigation-input-resolution.md`, `docs/architecture.md`, `docs/cli.md`, `docs/protocol.md`, and `docs/navigation.md` owner summaries with manifest complete-basename `extensions[]` suffixes/`filenames[]`, lexical routing pathname versus post-selection normalized document path, route-before-document-I/O sequencing, private pathname/format lookup state, component ownership, explicit override, exact diagnostics, no fallback, and complete probe deletion.
- [ ] 2.2 Update `docs/adapters/markdown.md` and `docs/adapters/json.md`; declare the approved pathname hints, preserve adapter-owned parse/ref/operation semantics, remove selection-probe obligations, document `.prettierrc`/`.code-workspace` as best-effort until their grammars are supported, and migrate JSON post-probe mutation semantics to normal document/JSON diagnostics.
- [ ] 2.3 Delete probe protocol constants/types/re-exports/decoders, runtime contract validator, typed-field consumer definitions, `docs/schemas/probe-result.schema.json`, examples, fixtures, schema indexes, candidate-failure/probe vocabulary, and conformance/release references after task 0.5 closes the removal inventory; migrate or record every discovered consumer as breaking impact rather than retaining inspection-only code.
- [ ] 2.4 Update testing strategy, current semantic Case ledgers, coverage mappings, and release verification documentation for the changed Current entities and observable behavior; do not use archived Cases or changes to invent unrelated coverage obligations.

## 3. Implement only the approved routing mechanism

- [ ] 3.1 Add manifest `formats[].filenames[]` and the approved initial JSON/Markdown hints without Cargo, lockfile or license-material changes; prove manifest remains the single routing metadata owner.
- [ ] 3.2 Derive validated exact-filename, ASCII-normalized suffix and format lookup views during registry construction, and implement one private lexical complete-basename helper with longest-suffix selection without a regex engine, custom inference trait, service object, plugin hook, scoring framework, generic detector registry, cache or second metadata store.
- [ ] 3.3 Implement the approved exact diagnostic table: no hint match before document I/O → `FORMAT_UNKNOWN / FORMAT_NOT_RECOGNIZED` with routing pathname; duplicate format identity → `registry-format-identity-conflict`; duplicate exact-filename/normalized-suffix hint → `registry-path-hint-conflict`; selected invalid JSON → `DOCUMENT_CONTENT_INVALID` with the four approved JSON reasons and normalized document path; preserve other explicit/selected adapter diagnostics and remove routing-only unsupported/ambiguous/candidate evidence.

## 4. Replace routing and fallback behavior

- [ ] 4.1 Replace registry-order candidate traversal with a two-stage path flow: before target-document filesystem I/O, match the lexical complete basename by exact filename first and otherwise by ASCII-normalized longest suffix, then exact-lookup the matched format/definition to select one adapter; only after selection, perform filesystem-backed path/access normalization and dispatch preparation.
- [ ] 4.2 Make registry construction/doctor/release validation reject duplicate normalized format identities, duplicate ASCII-normalized suffixes and duplicate exact filenames; retain defensive runtime global conflict ids; prove different-length suffix overlap uses longest match, exact filename precedence is deterministic, and registry order never chooses a winner.
- [ ] 4.3 Change explicit adapter selection to exact adapter-id lookup that bypasses pathname routing, then proceeds through post-selection document path/access normalization, selected-operation resolution and closed input construction without receiving matched format state.
- [ ] 4.4 Remove candidate failure accumulation and probe-stage routing state; add the `DOCUMENT_CONTENT_INVALID` code/details/reason mapping; update diagnostic canonical payloads and protocol schema/examples so Target details omit `candidate_failures`, probe stages/reasons, parser internals, and private pathname/index evidence.
- [ ] 4.5 Preserve the selected definition through dispatch and prove selected parse, semantic, operation, invalid-result, full-read, and nested behavior never re-enter pathname routing or dispatch another adapter.

## 5. Complete probe and adapter-owner migration

- [ ] 5.1 Remove `Adapter::probe`, `AdapterDefinition::probe`, selection-only `CandidateEvidence`/`CandidateStage`, probe version/reason/result types and all dead exports; do not retain an inspection-only surface.
- [ ] 5.2 Remove Markdown and JSON routing-probe implementations and migrate their owner tests to manifest suffix/exact-filename hints plus selected operation parsing without changing ref, outline/read/find/info, pagination, cost, or full-read semantics.
- [ ] 5.3 Remove probe decode/runtime contract validation, probe typed-field `FieldDefSet`s, schema/examples/fixtures/indexes and validation tests; prove no current protocol, adapter, CLI, release or conformance surface references probe.
- [ ] 5.4 Verify implementation matches the accepted `add-project-wide-find`, `reuse-adapter-document-state`, `add-ast-grep-code-adapter`, and `support-jsonc-in-json-adapter` handoffs; do not implement, rebase, or edit those changes under this task.

## 6. Targeted and workspace verification

- [ ] 6.1 Run formatting, targeted core-CLI/architecture/adapter-contract/navigation/diagnostics/protocol/contract-validation/typed-fields/Markdown/JSON unit and integration tests, route-before-I/O/path-failure tests, invocation-log tests, registry/doctor validation, and release smoke for every changed branch.
- [ ] 6.2 Run manifest/protocol schema, example and fixture validation plus searches proving all probe artifacts and routing-only candidate vocabulary are gone, compound-suffix/`filenames[]` rules and exact routing-versus-normalized path diagnostics match owner materials, and no matched hint/format or other derived routing state leaks into adapter input or public/log output.
- [ ] 6.3 Build and test the supported Linux GNU and Windows MSVC targets through the project-supported local/CI path; run canonical package verification and Markdown/JSON automatic plus explicit selection smoke from the manifest-resolved binary.
- [ ] 6.4 Confirm the final resolved dependency graph has no new routing crate; repeat binary/package size, startup and representative pathname-routing measurements against task 0 evidence, returning to human review if an approved assumption changed.
- [ ] 6.5 Run `bun run verify:docnav-workspace` and any owner-declared unified release validation required by the changed Rust/docs/schema/example/test surfaces.
- [ ] 6.6 Run `openspec validate replace-probe-traversal-with-inferred-routing --strict`, `dnm outline` for every change artifact and changed owner document, `git diff --check`, and a scoped final diff confirming unrelated worktree content was preserved.

## 7. Final review and archive readiness

- [ ] 7.1 Review the final diff against manifest-owned exact-filename/complete-basename suffix hints, route-before-document-I/O sequencing, ASCII-normalized longest-suffix selection, zero dependency, private match state, exact diagnostics, probe deletion, no-fallback semantics, minimal-implementation constraints and all cross-change handoffs; reject any content detector, regex/detector framework, second metadata owner, registry-order behavior, adapter-internal parser mapping owned by this change, or unapproved public contract.
- [ ] 7.2 Obtain final architecture/product-owner acceptance of verification and risk evidence, confirm main specs/docs/code/tests/release artifacts describe the same Current behavior, and only then assess this change for archive.
