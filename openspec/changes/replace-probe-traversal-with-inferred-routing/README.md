本 README 是 `replace-probe-traversal-with-inferred-routing` 的临时入口工件：它只定位本 change 的目标与 artifacts，不表示依赖、probe 兼容性或实施已经获批。

# replace-probe-traversal-with-inferred-routing

以单次格式推断和精确 registry 路由替代按注册顺序遍历 adapter probe，并在依赖与兼容性审计获人工批准后实施。
