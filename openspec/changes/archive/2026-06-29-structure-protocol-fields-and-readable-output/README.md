# structure-protocol-fields-and-readable-output

定义 Docnav 结构化协议字段与 readable 输出组织方式。

范围包括 protocol request/result、error/warning 投影、readable-json/readable-view、schema/examples、Rust protocol/diagnostics 类型、adapter SDK、Markdown adapter 和 readable renderer。
