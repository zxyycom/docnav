# add-obvious-result-auto-read

Default-on auto-read for `outline` and `find` when the current returned refs deduplicate to one, controlled by `--auto-read` or `defaults.auto_read` and composed from the existing `read` operation.
